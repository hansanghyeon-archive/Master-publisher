import { motion } from 'framer-motion';
import styled from 'styled-components';

const SMD = styled(motion.div)`
  width: 150px;
  height: 150px;
  background: #fff;
  border-radius: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default SMD;
